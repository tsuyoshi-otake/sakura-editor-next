# 正規表現ライブラリ bregonig.dll

製品がリンク・配布する `bregonig.dll` は
`third_party/owned/bregonig-next` の CMake ビルド成果物である。
ライセンス文は同じツリーの `bsd_license.txt` / `perl_license.txt` /
`perl_license_jp.txt` を同梱する。

`bron420.zip` は #185 で削除した。差分 golden は
`src/test/resources/tests1/bregonig/` にある。
