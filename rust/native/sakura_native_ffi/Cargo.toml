[package]
name = "sakura-native-ffi"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
name = "sakura_native_ffi"
crate-type = ["staticlib"]

[dependencies]
sakura-simd = { path = "../sakura_simd" }
sakura-unicode-core = { path = "../sakura_unicode_core" }
