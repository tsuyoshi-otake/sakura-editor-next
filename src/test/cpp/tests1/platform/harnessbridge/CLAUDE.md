# Harness Bridge tests

Keep protocol, endpoint, security, transport, and broker tests deterministic,
UI-free, and local. Use unique endpoint names and bounded deadlines. Never put
PTY text, message payloads, secrets, or launch arguments in diagnostics. Every
accepted request must have one terminal outcome, and transport shutdown must
join all workers before a test returns.
